spring.application.name=shopapp
# SERVER
server.port=8080

# MYSQL DB CONFIG
spring.datasource.url=jdbc:mysql://localhost:3306/online_shop?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Sravya@123J

# JPA & HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# CORS (Optional override in controllers)
spring.web.resources.static-locations=classpath:/static/

# CONTEXT PATH (Optional, for organized APIs)
# server.servlet.context-path=/api

# FILE UPLOAD (Optional if needed)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
